# Uncomment the next line to define a global platform for your project
  platform :ios, '9.0'

target 'SegundaMano' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SegundaMano
  pod 'Charts'
  pod 'TinyConstraints'

  target 'SegundaManoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SegundaManoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
